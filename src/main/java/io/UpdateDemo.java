package io;

/**
 * 修改昵称功能
 * 程序启动后，要求用户输入用户名和新昵称，然后将user。dat文件中该用户的
 * 昵称进行修改。如果输入的用户名不存在则输出“查无此人“
 *
 * 思路：
 * 首先循环读取user。dat文件中每个用户的用户名并进行比对，如果找到该用户
 * 则将指针移动大奥该条记录的昵称位置。然后将新的昵称转化为字节数组后扩容到32
 * 字节并写入来覆盖原昵称信息
 *
 */
public class UpdateDemo {





}
